# Full-stack product agent rules

- User outcome: [CORE OUTCOME]
- Frontend: [PATH + STACK]
- Backend: [PATH + STACK]
- Shared contracts: [PATH]
- Trace changes from UI intent through validation, persistence, and response.
- Change the shared contract once; do not maintain divergent duplicate types.
- Preserve permission checks on both client affordances and server enforcement.
- Include partial-failure and retry behavior for every network mutation.
- Avoid cross-layer refactors unless they are necessary for correctness.
- Verify the narrow path end to end plus layer-specific tests.
- Handoff: observable outcome, checks run, remaining risk, and anything not verified.
